

const Series = (nombre,descripcion,episodios,url,autor) =>{
       
    let templateSeries= 
    `
    <div class="container">
       <h1>Hola</h1>
    
    </div>
    `

    let newDiv = document.createElement("div")
    newDiv = templateSeries;

    return newDiv
}

export default Series